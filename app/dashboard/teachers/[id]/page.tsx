export const dynamic = "force-dynamic";
export const fetchCache = "force-no-store";

import TeacherProfilePageClient from "./page.client";

export default function TeacherProfilePage() {
  return <TeacherProfilePageClient />;
}
